# suppress inspection "UnusedProperty" for whole file
debug=true
spring.datasource.url=${locationmapper.datasource.url}
spring.datasource.username=${locationmapper.datasource.username}
spring.datasource.password=${locationmapper.datasource.password}
spring.datasource.driver-class-name=org.h2.Driver
spring.batch.job.enabled=false
doma.dialect=H2
workdir=locationmapper-test
mapquest.apikey=${locationmapper.mapquest.apikey}
cron.expire=${locationmapper.cron.expire}
fileRetentionPeriod=${locationmapper.file_retention_period}
bucketName=${locationmapper.storage.bucket}
cdnOrigin=${locationmapper.cdn}
